# GST & tax

**Status: planned.** Fully specified, no code yet.

The design is settled and recorded in `SPEC.md` at the repository root. This page will be written when the feature is built — per the [release-note rule](#/release-notes), documentation lands in the same commit as the implementation, so an empty page here means an unbuilt feature rather than a missing page.

See [Build status](#/status) for what does exist today.
